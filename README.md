A banking project which consists of loan prediction model, customer segmentation and project recommendation
Data are synthesized using faker and the data is loaded in the model
Place the app.py file and models folders in the same location after unzipping project_bundle and then run the uvicorn commands to start fastapi
